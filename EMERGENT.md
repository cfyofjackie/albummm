# EMERGENT — 意外长出来的功能

这里只记一类东西：**不是专门做出来的、而是由别的决定顺带成立的功能**。

每条固定写五段，重点是「当时是怎么实现的」和「现在靠什么成立」——
写清楚当时那版长什么样，是为了以后不要再沿着那条路重做一遍。

---

## 点中缝看中间 —— 跨中缝照片的居中取景（V0.6.4）

**现在**：点跨页主图 / 超宽横幅 / 三联中图，画面就移到那张照片上。它们的盒子中心**恰好落在书脊**，
所以「点中缝」看到的就是整张跨页构图的正中；点两侧则取景到被点的那一页。三者倍数完全相同。

**为什么算「意外」**：两条本来独立的决定相乘 ——
1. **排版事实**（V0.6 起）：跨中缝的照片画在一张 200% 宽的画布上、左右各显示一半，
   于是它的盒子中心正好是书脊；
2. **交互规则**（V0.6.3）：点哪张照片，就把那张的**中心**放到画面正中（视点平移，倍数不变）。

两条一乘，「点中缝看中间」自动成立 —— 代码里没有任何「如果点了书脊就……」的分支。

**当时是怎么实现的**（V0.4a → V0.6.4，追了十几个版本，一直做不出来）：
- 单位是「页」：Focus View 只能放大一页，被书脊切开的照片天然只剩一半。
  V0.4g 调「书脊侧露边」、V0.4h / V0.4j 调页宽与露边宽度，都是在围着「半张图」打补丁。
- V0.6 换成「换一个视图」来做：`SpreadFocusView`（整跨页放大）
  ＋ `isSpineHit` 热区（点在书本中线 ±max(18px, 书宽 3.5%) 内才触发）
  ＋ 三联中图另走一层「跨中缝细看」（把中图单独放大到 1.8×）。
- 因为是「换视图」，就必须有看不见的热区来触发（**触发点不明确**）、必须做视图之间的过渡
  （跳变、重影、「先缩一下」这些怪现象都从这来），而且三联中图会比左右图大一档
  （1.8× vs 1.15×），行为不可预期。
- V0.6.1 先删热区与特殊视图，行为统一了，但「看中间」也一起没了 —— 这一步是必要的清空。
- V0.6.3 换成「点哪张就居中哪张」，效果自动出现；V0.6.4 再对「盒子等于整页」的照片
  按落点细分（贴书脊 → 中间、点两侧 → 那一页），把单页取景补回来。
- 一共删掉 472 行（`SpreadFocusView.jsx` + `spread-focus.css`）。

**现在靠什么成立**：
- 规则：`SLC.md`「取景规则按照片宽度分两种」——比一页窄的居中自己；铺满整页的按落点分。
- 代码：`FocusView.jsx` 的 `anchorFor()` / `applyAnchor()`；照片盒子带 `data-photo-id`
  （`AlbumPage.jsx`）；书里点击时把「落点离书脊多远」一并传出（`BookView.jsx`）。
- 怎么量：`node check/spine-anchor.mjs`（页中心 / 书脊偏移 0–1px）。
- **失效条件**：如果 studio 的跨页不再是一张 200% 画布切两半、而是每页各自一张图，
  「盒子中心 = 书脊」就不成立，这个效果会自己消失。

---

## 追加新条目时

1. **标题**：功能名 + 现在生效的版本号；
2. **现在**：一句话说清用户看到什么；
3. **为什么算「意外」**：哪两条决定 / 事实相乘得到的；
4. **当时是怎么实现的**：旧做法、旧文件与函数名、为什么别扭（越具体越好）；
5. **现在靠什么成立**：规则在哪、代码在哪、怎么量、**什么情况下会失效**。
