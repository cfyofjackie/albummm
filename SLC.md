# SLC — 电子相册生成工具（第一版规格）

一个纯前端的电子相册生成工具：**上传 5–20 张图片 → 输入名称（可选）→ 选择风格 → 自动生成一本有出版物气质的数字相册**。

它应该看起来像摄影书、展览图录，克制而高级；而不是普通拼图相册、Canva 模板站、高饱和拼贴小志。

---

## 一、产品定位

这不是「编辑器优先」的产品，而是**相册生成器**：

> 用户几乎不需要编辑，上传完图片就能得到一本有审美、像成品的电子相册。

## 二、SLC 边界

### 只做核心体验

- 上传图片
- 输入相册名称（可选）
- 选择风格
- 自动生成相册
- 翻页预览
- 轻量修改（重新生成 / 修改标题 / 切换风格）

### 不做

登录系统、后端服务、云存储、AI 生图、每页手动输入文字、贴纸素材库、自由拖拽编辑器、抠图、复杂裁切、社交功能、多人协作。

---

## 三、页面比例与响应式（核心架构决策）

### 1. 固定设计坐标系与相册开本

每一本相册必须固定使用一种页面比例，**内部版式不随设备重新排版**——设备变化只影响整本书的显示大小，不影响页面内部构图。不能让每一页随照片比例改变，否则会失去书本感，也会破坏翻页模型。

默认开本为 **3:4 竖版摄影书**，同时提供：

- **4:3 横版摄影书**：旅行、风景、横图为主的相册。
- **1:1 方形摄影书**：图片比例混合、日常记录。
- **4:5 Editorial**：人像、精选图与更强杂志感。

响应式只体现在「展示方式和缩放策略」：

- **全端默认双页 spread**：两个同开本页面组成展开，整本书作为一个完整视觉对象（书脊 + 整体书影），不是两张卡片并排；手机端不退化成单页轮播。
- **手机端**（`< 768px`）：Book View 以容器**宽度**为主要约束；点按页面进入 Focus View（单页近全屏）阅读。
- **平板端**（768–1199px）：与手机同一模型，横屏以高度约束。
- **桌面端**（`>= 1200px`）：以容器可用**高度**为主要约束缩放 spread，左右留白；不要为撑满宽度导致页面高度过小。

### 2. 阅读模型：Book View / Focus View 分工

- **Book View 负责「像一本书」**：默认状态。封面独占起始视图 → 刚性 3D 翻页（绕书脊 rotateY，perspective ≈ 2000–2400px，约 650ms；翻动页带随角度变化的书脊侧阴影，下层页面有投影）→ 封底收尾。页面序列配对为 leaf：封面独占、内容页两两配对（**奇数补空白页**，印刷书惯例）、封底独占。
- **Focus View 负责「看清内容」**：点按 Book View 中的页面进入，单页近全屏横向 scroll-snap 滑动，**无翻书动画**；点空白处 / 下滑 / 关闭按钮退出，恢复到原 spread 位置。
- Book View 点击边界：书本**最外侧 12%**为独立翻页热区，始终覆盖在图片上；中央区域点击照片进入查看。这样满版图仍可稳定翻页，不依赖图片是否留白。
- 手机端 Book View 手势：左右滑动松手翻页（阈值触发完整动画，v1 不做拖拽跟随）；外侧热区作为点按翻页补充。
- **点开行为与「是否跨中缝」无关**：任何照片（含跨页主图、超宽横幅、三联中图）都进入**它所在那一页**的单页 Focus View——放大倍数、横向滑动、退出方式与双图白边跨页完全一致。跨页只影响排版，不改变点击行为。
- **取消中缝热区**：点击照片不再因为落点靠近中缝（跨页照片的中心往往正是中缝）而切换到另一种视图。
- Studio 三联中图多一层「跨中缝细看」：它在单页里跨过书脊，从单页 Focus 里点它时，先把整张双页纸放大、再就地放大这张照片，中缝仍穿过画面、左右书口退到屏外；三张图不拆成独立图片轮播。
- 21:9 等超宽图全部使用「完整跨页横幅」：跨越书脊但以 contain 保留全图，上下保留纸面，不能为填满高度裁掉两端。
- 书脊/gutter 用克制的渐变阴影表达；整本书直角、阴影轻微、精装感。
- 控制栏默认弱化/隐藏，点击书本外空白区域淡入。

### 3. 模板内部只使用相对单位

模板内部不使用绝对像素定位，只用 `%`、`aspect-ratio`、相对单位描述图片位置和尺寸。文字字号同样使用相对单位（容器查询单位 `cqw` 或 `em`），保证页面缩放时构图不破。

媒体查询只决定「单页还是 spread」，不触碰任何模板内部样式。

### 4. 跨页构图仅属于 Studio 摄影书模式

Gallery、Rhythm、Frame 的双页 spread 只是独立单页的左右并排渲染。只有 **Studio** 先以一张双页画布生成，再稳定拆成左右两页渲染；可使用跨页主图与三联跨页。

- 跨页主图只使用与当前 spread 比例接近的横图；否则宁可保留白边。
- 三联跨页只组合三张竖图，统一高度、按原图比例确定宽度；中间图可经过书脊。
- 重要人脸、文字或主体贴近中缝的照片不应自动进入 Studio 跨页；当前由用户选图与后续主体保护共同控制。
- 翻页仍然克制：**不做纸张弯曲 / page curl，不做拖拽跟随翻页**（滑动松手触发完整翻页动画）。中缝加一条细阴影/渐变模拟书脊。

---

## 四、图片比例适配

**相册开本不是图片框比例。** 用户上传的横图、竖图、方图优先保持原始宽高比，通过留白和不同模板适配，不统一强制裁切。

### 图片分类（排版前按宽高比）

- 横图：`w > h`
- 竖图：`h > w`
- 方图：接近 `1:1`
- 超宽横图：`w / h > 1.8`
- 超长竖图：`h / w > 1.8`

### 原则

- 优先保留原图比例、完整展示（contain）；仅少量强调型页面允许轻微裁切（cover）
- **纸张开本固定，不等于图片框固定。** 每张照片根据自身比例与所在版式的可用宽高自适应缩放、居中与留白；不能把所有图片塞进同一种固定框。
- 不同方向匹配不同模板；**不统一裁成某一种比例或方图；不自动旋转 90°**
- 超长竖图优先放进上下双图页或三横排页，避免在单图页大面积留白

### 单图页两种显示模式

- **Contain**（默认）：完整显示，四周允许留白
- **Cover**（少量强调页）：铺满页面，允许少量裁切；只有裁切不超过约 12% 时允许自动使用。否则使用 Contain，未来由用户主动决定是否裁切。

---

## 五、排版引擎

排版是一个**纯函数**：

```
planPages(photos, style, seed, format) → pages[]
```

输入图片（含尺寸分类），输出页面序列。可测试、可重跑（重新生成用可 seed 的伪随机）、换风格只需换参数重跑。

### 核心思路

**单图页为主，多图页为亮点。** 每页最多 3 张图。

| 上传数量 | 策略 |
|---|---|
| 5–7 张 | 以单图页为主，最多穿插 1 个双图页 |
| 8–12 张（推荐区间） | 单图页 + 双图页混排，可少量加入 1 个三图页 |
| 13–20 张 | 增加双图页比例，允许更多三图页，仍保留单图页维持节奏 |

### 节奏

整本书不要每页一样，要有呼吸感。单图页承担主视觉，双图页承担关联组合，三图页收编碎片/小细节图（含超长图）。

### 图片方向驱动模板选择（V0.5 排版语言）

- 竖图 → **优先单独成页**：居中 / 上顶（下方留白）/ 下顶（上方留白）/ 偏置；两张竖图并排的双图页已完全停用（竖版单页里并排显挤，那是「对页」语言，留给未来的 spread 排版）
- 横图 → 大单图页（含靠上/靠下变体）、两张横图**上下排**的双图页
- 方图 → 均可
- 超长图、碎片小图 → 三图页
- 双图页只由横图/方图对构成；混合对（一横一竖）拆回单图页

---

## 六、页面模板

- **单图页 ×6**：居中留白 / 接近满页（可 cover）/ 较小多留白 / 左右偏置 / **上顶留白** / **下顶留白**——竖图的节奏主力
- **双图页 ×1**：两张横图**上下排**（只由横图/方图对构成；竖图不进双图页）
- **三图页 ×2**：一大两小 / 三张横向排列（收编超宽横图）
- **封面 ×1**：纯色，只显示相册名称（居中或偏置标题，算一个模板内的变体）
- **封底 ×1**：呼应封面的纯色页 + 小字标题

### Studio 摄影书模板（跨页单位）

- **跨页主图 ×1（按条件）**：一张适配比例的横图铺满两页；仅允许极轻微的安全裁切，作为开篇或章节主视觉。
- **三联跨页 ×1（按条件）**：三张竖图同高横排，保留足够纸面与列间距；参考专业婚礼摄影书，而不是社交拼贴。
- **双图白边跨页 ×N**：左右各一张，独立留白、完整展示；用于把有关系的两张图并置。
- **题名页 + 单图 ×1（按条件）**：当余下一张图时，以简短书名页配一张完整照片收束。

Studio 的目标是“照片编辑后的成书感”，因此不加入贴纸、斜放图片、彩色卡片或 scrapbook 装饰。

## 七、视觉规则（严格遵守）

- 图片**不要圆角**，全部直角矩形
- 不斜放、不拼贴覆盖、不贴纸、不大面积装饰元素、不高饱和色块
- 审美来自：留白、图片尺寸关系、每页数量变化、页面节奏
- 内页默认**完全无文字**，不要求每页标题/说明

---

## 八、封面与书衣

封面是用户进入一本书的第一个记忆锚点，应支持照片与出版物材质的组合，而不使用外部来源图片。

- 用户可从已上传照片中选择一张作为封面；系统同时推荐候选照片
- 用户可选择纯色封面作为替代方案
- 用户可选择书衣颜色、轻纹理与阅读器外壳；它们围绕封面页和整本书出现，不覆盖内页照片
- 内置低饱和、有「书布感」的颜色：Oat 燕麦米 / Moss 苔藓绿 / Brick 砖红 / Ink 墨蓝黑 / Ochre 赭黄 / Fog 雾灰蓝 / Wine 暗酒红 / Paper 旧纸白
- 纹理只提供克制选项，例如纸纤维、轻颗粒、泡泡纱、泛黄纸；不使用厚重拟物或高频图案
- 低饱和、有灰度、不鲜艳、不用渐变；系统可推荐组合，用户可轻量切换

---

## 九、上传与交互

### 上传

- 拖拽 + 多选；jpg / jpeg / png / webp
- 推荐 8–12 张；最少 5 张；最多 20 张

### 首页

项目标题、一句说明、上传区、名称输入框（空则用 `Untitled Album`）、风格选择、生成按钮。

### 预览页（阅读器）

- 默认 Book View：整本 spread + 刚性翻页（见三.2）
- Focus View：点页面进入的单页滑动阅读
- 弱化的控制栏：页码指示、修改标题、切换风格、重新生成、返回编辑；点空白淡入
- 动画克制，流畅稳定优先；`prefers-reduced-motion` 时退化为直接切换

---

## 十、技术方向

- **Vite + React** 纯前端静态站，无需 Next.js（无 SSR/后端需求）；相册模板本身倾向手写 CSS 而非 Tailwind arbitrary values
- 数据全部在浏览器本地处理；预览用低分辨率版本（`createImageBitmap`），避免 20 张原图撑爆移动端内存
- 图片尺寸读取需感知 EXIF 方向，避免双重旋转

### 数据结构

```
Album  { title, style, format, coverColor, photos[], pages[] }
Page   { type: cover|single|double|triple|studio|back, layoutId, imageIds[], studio? }
Photo  { id, src, previewSrc, width, height, orientation }
```

### 架构分层

1. `planPages` 排版纯函数（布局逻辑）
2. 模板渲染层（视觉样式，百分比坐标系 + 相册开本容器）
3. 缩放展示层（单页/spread、宽度/高度约束）
4. 上传与配置 UI

---

## 十一、优先级

1. 默认生成结果要好看
2. 移动端体验要好
3. 不同尺寸图片排版不能乱
4. 界面简洁，风格不要模板味重
5. 先把 MVP 跑通，不过度追求功能完备
