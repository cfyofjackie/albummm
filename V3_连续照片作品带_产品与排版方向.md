# V3 — 连续照片作品带：产品与自动排版方向

**状态：** 方向草案，尚未进入实现

**定位：** 第三次产品探索；以自动生成的 Carousel 照片作品为核心。

## 0. 与已有文档的关系

本文件是独立的 V3 方向文档，不改写、也不覆盖以下两条已存在的探索线：

| 文档 | 原有方向 | 与 V3 的关系 |
| --- | --- | --- |
| `电子摄影书_产品与视觉方向.md` | 自动生成、双页翻阅的数字摄影书 | 保留为“摄影书”方向；V3 不沿用书架、封面、合书与翻页作为主体验。 |
| `PRD-照片拼贴-MVP.md` | 单张竖版拼贴成品 | 保留为“单张拼贴”方向；V3 的最小成品是一组连续页面，而非一张拼贴。 |

本次会尝试“胶卷 / 展开”的观看效果，但**胶卷不是 V3 的产品定义**。它只是其中一种预览与动效实验，未来可独立发展为另一款产品。

---

## 1. 一句话定义

> 用户上传一组照片后，不需要自己摆图，就得到一组有统一气质、可横向连续浏览、也可逐页发布的照片作品。

它不应被称为传统相册书，也不是 Canva 式 collage 编辑器。

更准确地说，它是：

> **先在一条连续画布上完成自动编辑，再切成若干独立 Carousel 页面。**

每一页能单独导出；从左到右浏览时，又能感到它们共同属于同一件作品。

---

## 2. 已确认的体验边界

- 首次流程：上传照片 → 选择 Gallery / Muse / Weekend → 自动生成 → 横向拖动预览 → 轻量重排 → 导出整组。
- 用户不需要先选模板、逐页排版或填写大量文案。
- 第一页没有强制“Hero”职责；它只是序列的第一种构图。视觉重点可以在任何一页出现。
- 系统可推荐更合适的序列变体；用户只能做轻量修正：重生成整组、替换某页构图、替换照片、调整顺序。
- 不做自由拖拽、自由缩放、贴纸库与复杂字体编辑。
- 一组作品的默认页数建议为 4–8 页；应支持少量照片也成立。输出尺寸暂不在本方向文档中锁定。

---

## 3. 视觉语言：背景先成立，照片后进入

参考图中“有点乱但好看”的关键，并不是照片本身被随机叠放，而是分成稳定的三层：

1. **连续底层**：贯穿整组页面的纸色、淡网格、低对比纹理或色带，提供统一的场景。
2. **页面节奏层**：页面之间保留同一条基线、边距和视觉重心；有的页密，有的页安静。
3. **照片物件层**：照片以大小差异、轻微错位与少量重叠被放进场景，而不是填满一组等大的格子。

这是一种“受控的松散感”。它必须满足以下规则，才能避免落入 scrapbook 或模板堆叠：

- 每页只保留一组主要观看对象；其余照片是陪衬或细节。
- 整组最多使用一到两种非常轻的材质，不同时叠加胶带、撕边、贴纸、手写字与颗粒。
- 允许小幅错位；Weekend 可以偶有极轻倾斜，Gallery 与 Muse 保持横平竖直。
- 照片之间可覆盖，但每页只允许少量、明确的覆盖关系，且不遮住重要主体。
- 连续感优先用背景、色块、细线、编号或留白建立；跨页面切开同一张照片只作为罕见且主体安全的特例。

---

## 4. 模板单位：序列模板，不是单页模板

V3 的模板最小单位是一条 **4–8 页的序列**，不是“某个左竖右横的固定页面”。

一条序列可由以下构图角色组成；角色描述的是照片之间的关系，不规定第一页必须更强：

| 构图角色 | 作用 | 可接收的照片 |
| --- | --- | --- |
| 单图锚点 | 在背景中放置一张完整、安静的照片 | 任意比例，以完整展示为主 |
| 横向窗口 | 容纳横图、风景、多人或空间感 | 横图、超宽图 |
| 竖向立柱 | 让人物、穿搭、建筑或竖向场景有呼吸空间 | 竖图、超长图 |
| 对照组 | 让两张照片形成上下、远近、人物与环境的关系 | 两张比例相容的照片 |
| 片段区 | 收纳细节、方图和生活碎片 | 2–4 张任意比例小图 |
| 安静区 | 用小图、留白或低对比色块控制速度 | 任意比例，或不放照片 |

第一轮只设计三条五页序列，用于验证自动排版，而非建立庞大的模板库：

| 序列 | 节奏 | 对应风格 |
| --- | --- | --- |
| Calm 5 | 单图锚点 → 对照组 → 单图锚点 → 安静区 → 对照组 | Gallery |
| Editorial 5 | 对照组 → 片段区 → 单图锚点 → 安静区 → 片段区 | Muse |
| Fragments 5 | 片段区 → 对照组 → 片段区 → 单图锚点 → 小图收束 | Weekend |

---

## 5. 不同横竖比例的自动排版规则

### 5.1 先做照片盘点，再选模板变体

系统先将图片按比例分为：横图、竖图、方图、超宽图、超长图，并统计整组的比例结构。随后从序列模板中选择兼容的变体，而不是把所有照片硬塞进同一批槽位。

| 照片结构 | 优先变体 |
| --- | --- |
| 竖图较多 | 以竖向立柱与单图锚点为主；横图做上下小景或片段。 |
| 横图较多 | 以横向窗口、上下对照为主；不要求虚构竖图位置。 |
| 横竖混合 | 竖图承担主体或人物位置，横图承担环境、时间与空间关系。 |
| 方图较多 | 进入片段区或小型网格，不强拉成长方形。 |
| 出现超宽图 | 独立成为横向安静段落，完整展示优先。 |

### 5.2 槽位必须有回退方式

模板不能只写“这里必须是一张竖图”。每个方向型槽位都要有回退：

- 缺竖图：竖向立柱变为两张小图、留白或安静色块；不把横图暴力裁成竖图。
- 缺横图：横向窗口收起，替换为单图锚点或片段区；不把竖图拉宽。
- 全部同方向：通过图片大小、位置、留白和页面密度建立变化，而不是强行制造另一种比例。
- 比例极端：默认完整展示；只有裁切损失可忽略、且主体安全时才允许轻裁切。

### 5.3 排版底线

- 不拉伸图片。
- 不为了满版而粗暴裁切。
- 人物、脸、文字和明显主体不跨导出页面的切线。
- 所有上传照片默认应被使用；系统通过分组与页数调节密度，而不是静默丢图。
- 同一组照片、风格和种子应得到同一结果；“重新生成”只换组合与构图变体。

### 5.4 当前随机排版实验的临时尺寸规则

以下参数已在“尺寸标尺”原型中确认，先作为受控随机排版的硬边界使用；后续会用更多真实照片继续校验，而不是允许算法无限缩小照片来塞满页面。

| 项目 | 当前规则 | 处理方式 |
| --- | --- | --- |
| 最小图 | 照片内容短边不低于页面短边的 15% | 不能放下时顺延到下一页。 |
| 最大横图 | 照片内容宽度不超过页面宽度的 80% | 保留背景与留白。 |
| 最大竖图 | 照片内容高度不超过页面高度的 78% | 防止整页被单张竖图吞没。 |
| 白边 | 随卡片大小变化，2–5px | 小图更细，大图有轻微装裱感；不再固定为 7–8px。 |

---

## 6. 三种气质的差异

### Gallery

暖白、雾灰或极淡纸纹背景；单图与安静区比例更高；画面规整，文字几乎消失。松散感来自尺寸与留白，而非重叠。

### Muse

奶油、灰粉、酒红、鼠尾草绿、雾蓝等低饱和背景；允许小 serif、细线、日期与不对称构图。照片之间有轻微时尚 editorial 的对照，但不使用甜美贴纸或堆砌装饰。

### Weekend

可使用更可见的网格、色带或纸张感；片段区更多，照片的尺度变化更快。保留清晰网格与留白，避免成为典型手账模板。

三种风格共享相同的间距、图框、背景强度和文字规则；不同的是节奏与强调色，而不是换一整套不相干的模板。

---

## 7. 胶卷实验的正确位置

“卷起 → 展开 → 横向拖动”可以作为作品预览的进场方式：生成后看见一小段收拢的连续带，打开时舒展开来。

它不进入导出图片本身，不要求出现齿孔或仿真 35mm 胶片，也不限制每格照片的比例。若这套动效被验证确有独立吸引力，再将它从 V3 中抽离成单独产品探索。

---

## 8. 第一阶段验证目标

1. 用真实混合比例照片验证三条五页序列是否都自然。
2. 先验证“连续背景 + 照片物件层”的视觉效果，而不是先开发复杂胶卷动画。
3. 验证用户是否能在不编辑的情况下直接接受生成结果；不满意时只需一次“换一组排法”。
4. 验证单独导出的每页是否仍然完整，而非只有拼成横带才好看。

**完成标准：** 同一条序列面对全竖、全横、横竖混合和方图偏多的照片集，都没有明显硬塞、强裁切、重复或模板感过重的问题。
