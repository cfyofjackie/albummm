# PROGRESS — 开发版本记录

按小版本推进，每个版本结束时都应是一个可运行、可手动验收的状态。

---

## V0.1 — 骨架与上传 ✅

- [x] Vite + React 项目初始化，目录结构（排版引擎 / 模板渲染 / 缩放展示 / UI 四层分离）
- [x] 首页基础布局（Mobile First）：标题、说明、上传区、名称输入、风格选择、生成按钮
- [x] 图片上传：多选 + 拖拽，jpg / jpeg / png / webp，5–20 张限制与提示，缩略图删除
- [x] 本地读取图片，生成低分辨率预览（createImageBitmap），读取宽高并按方向分类（横/竖/方/超宽/超长）

## V0.2 — 排版引擎 ✅

- [x] `planPages(photos, style, seed) → pages[]` 纯函数
- [x] 数量自适应策略（5–7 / 8–12 / 13–20 三档）
- [x] 图片方向驱动模板选择，节奏编排（单图页为主，双/三图页穿插）
- [x] 可 seed 伪随机（mulberry32），支持「重新生成」出不同结果
- [x] 纯函数单元测试（vitest，18 项：结构不变量、三档数量、全横/全竖/混合、边界 5/20、确定性、风格差异、开篇图规则）

## V0.3 — 模板渲染 ✅

- [x] 模板坐标系：4:5 容器 + 百分比定位 + 相对字号（容器查询单位 cqw）
- [x] 单图页模板 ×4（center / full / offset 左右 / small，contain）、双图页模板 ×3、三图页模板 ×2
- [x] 封面 / 封底（纯色书布 + 名称，8 色低饱和色板，按 seed 确定性分配）
- [x] 三种风格参数落地：Gallery（8% 留白）/ Rhythm（4.5% 留白，满页强调）/ Frame（11% 留白 + 白卡纸装裱）
- [x] 最小预览器：翻页按钮 + 切风格 + 重新生成 + 返回编辑
- [x] `?demo=N&go` 演示模式：自动生成带编号的多方向测试图并成书（走真实 loadPhoto 管线）
- [x] 浏览器实际验收：封面 / 三风格内页 / 双图 / 三横排（超宽图中位）/ 封底均正确

## V0.4 — 书本化预览与翻页 ✅

- [x] `book.js`：pages[] → leaves[][]（封面/封底独占，内容页两两配对，奇数补空白页）+ flat 索引映射 + 单测
- [x] BookView：8:5 双页 spread、书脊 gutter、整体书影、刚性 3D 翻页（rotateY + 阴影随角度，prefers-reduced-motion 退化）
- [x] 手机手势：左右半屏点击翻页、滑动松手翻页、点页面本体进 Focus View；桌面键盘方向键
- [x] FocusView：scroll-snap 单页近全屏滑动（无翻书动画），点空白/下滑/按钮退出，恢复原 spread 位置
- [x] AlbumViewer 改造为阅读器壳：控制栏默认显示、点空白切换淡入（页码/切风格/重新生成/返回编辑）
- [x] 缩放：全端 spread，手机宽度约束、桌面高度约束；AlbumPage 固定坐标系不动
- [x] 浏览器验收：桌面翻页动画中间帧自然、手机 spread 保留、Focus 进入/滑动/退出恢复位置全部正确

## V0.5 — 打磨与验收

- [ ] 真机移动端过一遍核心流程（上传 → 生成 → 翻阅）
- [ ] 大图 / 小图 / 超长图混合上传的排版抽查
- [ ] 生成结果整体审美调优（留白、节奏、字号）
- [ ] 静态部署（GitHub Pages 或同类）

---

## 之后（backlog，不进第一版）

跨页中缝构图（spread 级排版）、照片顺序手动调整、更多风格、导出 PDF/长图、分享链接、装订手性改印刷惯例（第一张内容页放右页：`[空白|p1]、[p2|p3]…`）、横屏手机的页面尺寸（高度约束导致页面过小，需媒体查询单独处理）。

---

## 更新日志

### 2026-09-11
- 建立 `SLC.md`（融合初版 idea 与页面比例/响应式修订意见）
- 建立 `PROGRESS.md`，规划 V0.1–V0.5
- **V0.1 完成**：Vite + React 骨架、上传流程、图片读取与方向分类，构建通过并推送到 GitHub
- **V0.2 完成**：`planPages` 排版引擎 + 18 项单元测试全绿；开篇图（最大面积）改为在多图页分配前锁定；方向驱动的双图/三图页配对（竖图→左右双图、横图→上下双图、超宽图优先收编三图页）
- **V0.3 完成**：模板渲染层 + 最小预览器 + 演示模式；浏览器截图验收三风格成书效果通过
- **V0.4 完成**：阅读模型升级为「书本对象」——全端双页 spread、刚性 3D 翻页（不用库、不做纸张弯曲）、手机 Focus View 单页滑动、leaf 装订纯函数 + 单测；SLC.md 第三节/第九节同步修订为 Book View / Focus View 模型
- **V0.4a 完成**：Focus View 重做为「放大看这本书的一页」——FLIP 缩放动画（从被点页面原地放大/缩回，无界面切换感）、物理正确的书页几何（右页靠右书脊在左、左页镜像、邻页露出一小条、中缝阴影）、背景与书景全程一致（纸色）、退出后书同步摊开到当前页所在 spread
- **V0.4b 完成**：修「分段展开」——morph 移入 useLayoutEffect（初始态绘制前就位，消除终态闪现）、StrictMode 防重入、砍掉邻页两段式淡入淡出（整本构图随缩放连续变化）；Focus 中缝改为与书景一致（页面贴住 + 1px 书脊线 + 同款渐变）；翻页/退出动画增加 animationend 失联超时兜底（节流环境不再卡死）
- **V0.4c 完成**：用 FFmpeg 逐帧分析用户录屏确认残留问题（被点页有动画、邻页瞬移就位）后，FLIP 从「单页」升级为「容器级」——transform 作用于整条书芯 track，双页 + 中缝 + 露边作为一个连续对象从书中矩形整体放大/缩回，结构上杜绝先后顺序；morph 期间忽略 scroll 误判
- **V0.4d 完成**：修闪屏——V0.4c 把入场方向写反了（实际播放的是从焦点布局缩到书、再瞬间弹回）；改为标准 FLIP 顺序：无过渡摆到书中起始态 → 强制回流 → 开过渡归位；退场结束时保留终态 transform 直到卸载，消除缩回后的闪回
- **V0.4e 完成**：修退场接缝——书芯缩回终点会带出书中没有的邻页边和不同的阴影质感，直接卸载会跳变；改为缩到位后整层 150ms 快速淡出，溶进底下已对齐的书里
- **V0.4f 完成**：书端页不参与露边——放大第一/最后一张内容页时封面、封底不再探出一条色块（用户发现的 Fog 封面色条问题），改为贴视口边缘对齐、完全藏在屏外；内部页面互相露边保持不变
- **V0.4g 完成**：放大构图改为「书脊加权」镜像对称——放大任一页时书脊侧露出邻页一条（中缝语境），外缘一侧完全留空（用户反馈的白条 = 外缘邻页的纸边+阴影；左页/右页行为从此镜像一致）；外缘邻页被 margin 推出屏外后，白页边缘的阴影干涉闪烁源同时消失；封面/封底隐藏逻辑由该 margin 自然涵盖（移除 V0.4f 特例）
- **V0.4h 完成**：放大页宽度上限 78vw → 88vw（用户反馈外缘留空过宽、手机阅读面积损失）——手机上外缘空隙 66px → 27px，页面积 +28%；桌面宽屏不受影响（宽度由高度约束主导）
- **V0.4i 完成**：修退场时邻页图片延迟上屏（用户反馈「右边加载得比较慢」）——视口外的条带图片浏览器会推迟解码，缩回时邻页以空白页滑入、图片晚一步出现；Focus 打开时预解码全部条带图片（img.decode）
- **V0.4j 完成**：书脊侧邻页露出条 5vw → 8vw（用户反馈想多看到一点邻页），页宽 88vw → 86vw 微调平衡，外缘空隙基本不变
- **V0.4k 完成**：修翻页时书脊阴影消失——翻动页的阴影原先是 0 透明度渐入（起翻瞬间盖住静态书脊阴影）且背面渐变方向写反；改为翻动页两面常驻与静止 gutter 同款的渐变（阴影属于页面本身，全程连续）；照片加载时统一预解码，翻页揭示新页不再延迟上屏
- **V0.4l 完成**：修缩回时右侧白底晚到——缩回是新暴露区域的栅格化有延迟（预解码治不了），改为淡出与缩回同步进行的交叉溶解，白底瞬间被底下已就位的书页掩盖；录屏验证退场全程照片在场
- **V0.4m 完成**：重做 Focus 进出的淡入淡出分层与时序（用户反馈「放大/缩小都很奇怪」）。用 Chrome CDP 逐帧录屏 + rAF 几何轨迹复核，定位到三个问题并修掉：①入场时整层纸色底不透明、书在点击瞬间消失，页面在空白里放大（无来源感）；②退场时整层（含页面本身）一起淡出，与底下的书形成重影/溶图（桌面尤其明显，三张页叠在一起）；③控件凭空弹出/消失。改法：把纸色底拆成独立的 `.zfocus__scrim`（track 内容全程不透明），入场底 150ms 淡入——最初几帧书芯副本与书中矩形逐像素重合，书仍可见，页面像是从书里被拎出来；退场底延迟 220ms 再淡出——页面落回书中矩形时书才露面，交接处几何完全一致（实测末帧 = 书中布局，无跳变）；✕/页码淡入淡出；入/退场分别用慢起步与快回位两种缓动曲线（残影峰值从「整页重影」降到 ≤4.5% 面积）；`will-change` 提前到回流前，避免首帧掉回主线程
- **部署上线**：GitHub Actions 自动部署到 GitHub Pages（push main 即发布，构建 base=/albummm/）；仓库转为 public（免费计划私有仓库不支持 Pages）——线上地址 https://cfyofjackie.github.io/albummm/ ，演示模式加 `?demo=12&go=1`
- **V0.4n 完成**：放大视图翻页改为自管分页器（用户反馈甩动容易一次滑过两页）——弃用原生 scroll-snap + 惯性（无法限制一次手势滑过几页），横向拖动由 JS 接管跟手，松手后无论甩多用力都只翻一页；未过阈值（48px）弹回当前页；下滑退出、点邻页跳转、进入定位全部改走自管定位函数；顺带修复 React passive touchmove 下 preventDefault 无效的问题（原生非 passive 监听接管）
- **V0.4o 完成**：修放大视图「透过缝隙看到底下的书」——V0.4n 重写时丢失了 `is-open` 类的添加，纸色 scrim 一直是透明态，底下的书本从所有缝隙透出、✕ 和页码隐形（用户手机录屏实锤）；恢复入场添加 `is-open`、退场移除，浏览器验证 scrim opacity=1、控件显形
