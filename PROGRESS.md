# PROGRESS — 电子摄影书 / 数字相册

最后更新：2026-09-12

相关方向文档：[电子摄影书_产品与视觉方向.md](电子摄影书_产品与视觉方向.md)

## 当前结论

项目的核心闭环已经成立：用户可在浏览器本地上传图片，选择风格与相册开本，并自动生成一本电子相册；随后可翻阅整本书，并点击照片进入单页放大阅读。

当前版本的定位应保持为：**照片自动编辑器 / 数字摄影书生成器**，而不是自由排版编辑器或 scrapbook 工具。

## 当前状态

**版本：V0.6（可用原型，正在真实照片验收）**

本次核查结果：

- `npm test`：3 个测试文件、34 项测试全部通过。
- `npm run build`：生产构建通过。
- 当前项目是 Vite + React 的纯前端静态应用；无登录、后端或云端图片处理。

## 已实现功能

### 1. 创建相册

| 能力 | 当前实现 |
| --- | --- |
| 图片输入 | 支持点击多选与拖拽上传。 |
| 图片格式 | JPG / JPEG、PNG、WebP。 |
| 图片数量 | 最少 5 张、最多 20 张；推荐 8–12 张。 |
| 本地处理 | 浏览器读取图片尺寸并生成低分辨率预览；图片无需上传到服务端。 |
| 基础信息 | 可填写相册名称；空值使用 `Untitled Album`。 |
| 风格选择 | Gallery、Rhythm、Frame、Studio 四种风格。Studio 是以跨页为单位编排的摄影书模式。 |
| 重新生成 | 同一组图可重新生成不同排版；支持按风格重新排版。 |
| 封面 | 当前为低饱和纯色书布感封面，按随机种子分配颜色并显示相册名称。照片封面、纹理与外壳选择已确认进入下一阶段。 |

### 2. 自动排版

| 能力 | 当前实现 |
| --- | --- |
| 相册开本 | 整本书固定使用一种开本；支持 3:4 竖版（默认）、4:3 横版、1:1 方形、4:5 Editorial。 |
| 图片分类 | 横图、竖图、方图、超宽图、超长图。 |
| 图片展示 | 纸张开本固定、图片框自适应：以完整展示（contain）为主；仅当照片比例与开本接近、裁切不超过约 12% 时才允许自动满版。超宽图作为居中的完整跨页横幅。 |
| 页面节奏 | 单图页为主，穿插双图、三图页；根据 5–7、8–12、13–20 张三档调整密度。 |
| 竖图逻辑 | 竖图优先单独成页，可居中、上顶或下顶，利用留白建立节奏。 |
| 双图逻辑 | 双图页只由横图 / 方图组合，上下排列；竖图不再被拥挤地并排。 |
| 超宽与碎片图 | 适合被收编进三图页；大图与小图的关系由固定模板控制。 |
| Studio 摄影书模式 | 跨页先于单页生成：比例适合的横图可做跨页主视觉；三张竖图可组成统一高度的三联跨页；其余照片以双图大白边跨页或题名页 + 单图收尾。 |
| 可预测性 | `planPages(photos, style, seed)` 为纯函数；同一 seed 得到同一结果。 |

### 3. 阅读与交互

| 能力 | 当前实现 |
| --- | --- |
| Book View | 全端以双页 spread 呈现，包含封面、内容页、封底、书脊与轻阴影。 |
| 翻页 | 刚性 3D 翻页动画；支持左右半屏点击、左右滑动与桌面方向键。 |
| Focus View | 点击书页中的照片后，进入单页近全屏阅读。 |
| 点击边界与查看层级 | 书本最外侧 12% 是独立翻页热区，覆盖在图片之上；中央区域点击照片进入单页 Focus View。**点开行为与照片是否跨中缝无关**：跨页主图、超宽横幅、三联中图与双图白边跨页一样，都放大自己所在的那一页（同样的倍数与滑动），不再有「中缝热区」。只有三联中图在单页 Focus 里再点一次，才进入保留书脊的「跨中缝细看」。 |
| 单页浏览 | Focus View 中横向拖动，单次手势最多前进或后退一页；可关闭并回到原 spread。 |
| 阅读控制 | 页码、切换风格、重新生成、返回编辑；点击空白区域可弱化 / 显示控制栏。 |
| 动效兼容 | `prefers-reduced-motion` 下退化为更少动效。 |

## 已验证的质量边界

### 自动排版测试

34 项测试覆盖：

- 5–20 张图片的输入边界。
- 封面开头、封底结尾、每张图片恰好使用一次。
- 每页 1–3 张图片的结构规则。
- 三种数量区间的页面密度。
- 三种风格的差异。
- 超宽、超长、横图、竖图、方图混合场景。
- 竖图不进入双图页、横图双图页使用上下排列。
- 四种开本的比例配置，以及自动满版不会用于比例不适配的照片。
- Studio 跨页的完整配对、图片不重复消耗，以及跨页主图 / 三联图 / 双图留白模板。
- 同 seed 的确定性和不同 seed 的排版差异。
- 封面 / 内容页 / 封底的装订与 Focus View 索引映射。

### 仍应做的人工验收

- 真机走一遍：上传 → 生成 → 翻页 → 打开单页 → 返回。
- 用真实的全竖图相册、全横图相册、超长图混合相册检查节奏。
- 在小屏手机、平板横屏、桌面宽屏各检查一次页面大小和手势。
- 以真实用户照片评估自动生成结果，而不只依赖测试图。

## 与视觉方向文档的对应关系

当前代码已落实了“摄影书骨架”，但尚未进入“编辑层”。

| 视觉方向 | 当前完成度 | 当前状态 |
| --- | --- | --- |
| 照片优先、直角、留白、低饱和 | 已落实 | 核心视觉规则已在模板与封面中生效。 |
| 单图为主、多图为节奏 | 已落实 | 常规模式以单页为单位；Studio 模式补充了跨页主图、三联与双图留白节奏。 |
| 女性 editorial 的章节色与标题力度 | 未开始 | 后续应作为可选主题，而非默认强风格。 |
| 日期、地点、图注、色样 | 未开始 | 可作为轻量编辑层。 |
| 明信片、时间线 / 日历页 | 未开始 | 可作为相册的特殊章节页。 |
| 统一影调与纸张纹理 | 未开始 | 应谨慎加入，避免盖过照片。 |
| 人物主体保护 | 未开始，且为可选探索 | 只有确认智能裁切有真实需求时再评估本地检测方案。 |

## 当前限制与已知缺口

这些不是当前版本的故障，而是下一阶段的产品边界：

- 相册只存在于当前浏览器会话；刷新页面后不会恢复。
- 使用的是低分辨率预览图，尚未提供高质量导出。
- 用户不能手动调整照片顺序、替换某一个页面的图或指定某页版式。
- 阅读器中不能直接改相册标题，需要返回编辑页。
- 当前封面只能使用系统分配的纯色；封面照片、手动颜色、纹理和阅读器外壳尚未实现。
- 尚未提供文字、日期、地点、章节、明信片、日历、滤镜或 Live Photo。
- 尚无分享链接、PDF / 长图导出、登录、云端存储或协作能力。
- Studio 是目前唯一使用 spread 级构图的模式；它暂未基于人物主体位置避开中缝，也没有把跨页模板开放给用户逐页指定。

## 接下来建议的版本

### V0.6 — 开本系统、真实照片验收与视觉打磨

目标：确认自动生成结果在真实照片中稳定好看。

- [x] 实现相册开本选择：3:4 竖版、4:3 横版、1:1 方形、4:5 Editorial；3:4 为默认。
- [x] 实现自动满版保护：比例不合适的图片默认完整展示，不自动粗暴裁切。
- [ ] 完成真机与多尺寸屏幕验收。
- 收集至少 5 组真实相册样本：旅行、日常、人物、全竖图、混合比例。
- 调整留白、主图比例、双图 / 三图出现频率和字号。
- 实现封面选择：用户可选一张已上传照片作为封面，并选择书衣颜色、轻纹理与阅读器外壳；系统同时给出推荐组合。
- [x] 增加 Studio 摄影书模式：跨页主图、三张竖图三联跨页、双图大白边跨页与题名页 + 单图收尾。
- 用真实照片确认 Studio 中缝安全：人脸、文字与重要主体不应落在中缝；不适合跨页的横图保留白边。

完成标准：不同类型照片均能生成没有明显拥挤、裁切错误或重复节奏的成书结果。

### V0.7 — 轻量编辑与叙事层

目标：让用户补充记忆信息，但不把产品变成自由编辑器。

- 在阅读器中直接修改书名。
- 允许调整照片顺序，或对单页执行“换一种版式”。
- 增加受限文字模块：章节标题、日期地点、短图注、后记。
- 引入从照片提取的低饱和章节色 / 色样。

完成标准：用户可以修正自动生成的少数不满意页面，并让一本书具有私人叙事，不需要拖拽排版。

### V0.8 — 出版物细节与输出

目标：强化可收藏感与分享价值。

- 明信片页、时间线 / 日历页等特殊页面。
- 一键统一影调（以描述性影调命名，不仿制具体相机品牌）。
- 封面颜色选择与可选的轻纹理主题。
- 导出长图 / PDF 或分享链接；输出规格确认后再实现高分辨率渲染。

### 可选探索（不进入当前主线）

- 人物主体保护：仅用于本地智能裁切与避免跨页切脸，不做人脸身份识别。
- Live Photo：静态封面图，用户点击后播放短片段。
- 人物主体保护：仅在用户开启的前提下，辅助判断跨页中缝安全区与裁切保护。

## 需要产品确认的决策

1. **封面策略已确认。** 支持用户选择照片封面，并选择书衣颜色、轻纹理和阅读器外壳；系统可推荐组合。当前纯色封面保留为一种可选封面方案，而非唯一方案。
2. **输出目标尚未确定。** 若未来主要是网页阅读，可继续使用预览图策略；若要打印或导出 PDF，需要重新设计原图保留、内存控制与高分辨率渲染方案。
3. **编辑深度需要守住边界。** 推荐先做“换版式 / 调顺序 / 短文字”，不要进入自由拖拽画布。

## 历史里程碑

- V0.1：上传、多选拖拽、本地图片读取、尺寸与方向分类。
- V0.2：可测试的自动排版引擎。
- V0.3：页面模板、三种风格、封面与演示模式。
- V0.4：双页书本阅读、翻页、Focus View 与移动端手势。
- V0.5：竖图优先单独成页；双图页只采用横图 / 方图的上下组合。
- V0.6：相册开本选择与比例安全的自动满版；默认开本从 4:5 调整为 3:4；新增 Studio 摄影书跨页模板。
- V0.6.1：点开行为与「是否跨中缝」解耦——取消中缝热区与「跨中缝照片点开变独立页面/独立照片」的特殊处理，跨页主图、超宽横幅、三联中图与双图白边跨页一样放大自己那一页（1.87×、可左右滑动、同一套进出场）；三联中图的「跨中缝细看」改为单页 Focus 里的第二层；顺带补上跨页视图原本缺失的缩回动画，Focus / 跨页的纸色底与控件单独淡入淡出（页面本身不再参与淡出，消除重影）。
