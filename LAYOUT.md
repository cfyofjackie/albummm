# LAYOUT — 排版模板规范（v1 · 已定）

这份文档规定**排版模板有哪些、各收什么图、随机怎么随**。作用于**摄影书（studio）风格**，其余风格不动。

> v1 变更：7 条待确认已全部答复并写进正文；新增 T8 的构成说明；T1 门槛收紧。

---

## 1. 四条原则

1. **模板模块化**：每个模板是一个独立的「排版单位」（占一页或一整个跨页）。模板**不与「第几页」绑定**——「第 1 页永远是跨页」这种写法要拆掉。
2. **按适合度准入，不强行铺满，不强制裁切**：每张图先算出它**能进哪些模板**，生成时只从合法模板里选（模板是"根据上传的图片自动匹配"的结果）。**宁可留白，不裁比例**——开本也不跟着图片比例走。
3. **随机但受限**：种子决定「哪些图组成一组」「模板以什么顺序出现」，随机幅度**允许换模块**（同一张图这本做三联、那本做竖图大图）。同一批图，两本书的排列应当不同；但随机**不许破坏**第 5 节的底线。
4. **只改排版，不动阅读层**：模板决定「书里怎么摆」，放大阅读仍是那一档自适应倍数 + 视点平移，与模板无关。

---

## 2. 单位与术语

| 术语 | 含义 |
|---|---|
| 单页 page | 一张相纸 |
| 跨页 spread | 展开的左右两页，中缝（书脊）在中间 |
| 模板 template | 一个排版单位：1 张图跨两页 / 2 张图各占一页 / 一页上下两张…… |
| 准入 fitness | 某张图能不能进某个模板（由宽高比、尺寸、是否可裁切决定） |
| 节奏 rhythm | 模板序列的约束：同一种不连续出现、整本分布别太偏 |

---

## 3. 模板清单（v2：收窄到 6 个）

Studio 是偏专业的摄影书，模板不求多，只做下面 6 个。现状列：**有**＝已实现；**新**＝要新增。

| # | 模板（你的叫法） | 代码 id | 构成 | 准入 | 现状 |
|---|---|---|---|---|---|
| 1 | **三张图片跨两页** | `studio-triptych` | 三张横跨两页（现在的 2/7/9），统一高度、按各自比例定宽，中间那张可压书脊 | 三张比例相近的图（中间那张主体不宜正中） | 有（**一本一次**） |
| 2 | **一张图跨两页** | `studio-hero` / `studio-inset` / `studio-panorama` | 一张图横跨两页，按比例自动选一种处理：**满版**（比例≈1.5、裁切可忽略）/ **留白**（四边等宽 6%，contain）/ **超宽横幅**（宽度顶满、上下留白） | 横图（超宽走横幅；比例接近 1.5 走满版；其余走留白） | 有（三种都已实现并验证） |
| 3 | **一张图在左边** | `page-single-left` | 一张图放在**左页**，右页留白 | 任意 | **新** |
| 4 | **两张图一页** | `page-stack-2` | 两张图上下拼在**同一页**（统一高度、按比例定宽、共用中轴、等距），对页留白 | 两张能在这页叠下的图（见尺寸准入） | **新**（Gallery 有类似版式，studio 要补） |
| 5 | **一张图一页** | `page-single` | 一张图独占一页、放得大、四周留白 | 任意 | 部分有（Gallery 的 `single-*`；studio 要补） |
| 6 | **1:1 图横跨一页、右边再占一部分** | *待定* | 一张方图骑在书脊上，左页占满、右侧再吃掉一部分 | `1:1` 的方图 | **新**（构成待确认，见下） |

**相对 v1 砍掉的**：左右各一张（`studio-pair`）不再单列 —— 它等于"一张图一页"×2；一页三张（原 T8）、单页满版（原 T9）也先不做。

**多图模板的对页**：一页放多张时，对页保持纸面留白（作品集的克制感）。

**图片说明文字**：参考图里每张图下面有一行小字（地点）。我们现在图下显示的是尺寸占位。**记在这里，本版不做**。

封面 / 封底不属于模板，仍固定为第一页和最后一页。

### 待确认（决定第 3、5、6 个模板怎么做）

- **③ vs ⑤**：`一张图在左边` 与 `一张图一页` 是两种不同东西吗？我的理解：**③ = 照片只占左页、右页留白**（一个跨页放一张）；**⑤ = 一张照片独占一页**（左右页各一张时就是"左右各一张"）。如果是这样，③ 和 ⑤ 的差别只在"对页是否留白"。
- **⑥ 的构成**：方图是**骑在书脊上**（一半在左页、一半在右页，含右侧一部分），还是**占满左页 + 右侧再压一条**（跨过书脊但主体偏左）？我按前者理解，但这条得你确认再动手。

---

## 4. 按比例分配"可用模板"（准入表）

图片分类沿用 `SLC.md` 的定义：横图 `w>h`、竖图 `h>w`、方图 ≈1:1、超宽 `w/h>1.8`、超长竖 `h/w>1.8`。
**开本固定 3:4（2026-09 定），所以跨页比例固定为 1.5**——T1 满版的准入因此就是一条规则："图的比例接近 1.5（容差内）"；所有盒模型（T2 等宽内缩 / T4 三联 / T6·T8 竖排）只需按 3:2 设计一次。
下表是**按图片方向**给的允许集合（可多选，生成时按权重随机）；括号内是我的建议权重。

| 图片类型 | 可用模板（权重） |
|---|---|
| 超宽 `w/h>1.8` | T3 超宽横幅 (10) · T1 跨页满版 (2) |
| 宽横 `1.2–1.8` | T1 跨页满版 (5，仅裁切可忽略时) · **T2 跨页留白 (6)** · T6 一页上下两张 (5) · T9 单页满版 (2) |
| 近方 `0.9–1.2` | T6 一页上下两张 (5) · T9 单页满版 (3) · T7 左右各一张 (4) · T4 三联 (3) |
| 竖图 `1.2–1.8`（高/宽） | T5 竖图大图 (7) · T7 左右各一张 (4) · T4 三联 (5) · T8 一页三张 (1) |
| 超长竖 `h/w>1.8` | T5 竖图大图 (10) |

多图模板需要**同一组的图都能进**才成立（例如 T4 要求三张都允许 T4）。

---

## 5. 随机模型

```
plan = f(photos, style, seed, format)  +  overrides(用户逐页改过的模板)
```

生成流程：

1. **分类**：按宽高比给每张图打标签；
2. **算准入**：每张图得到一个「可用模板集 + 权重」；
3. **分组**：把照片池分组成 1 张 / 2 张 / 3 张的模板单元，**分组方式用种子随机**（现在是按上传顺序硬切）；
4. **排序**：模板序列用种子洗牌（现在是固定顺序：主图 → 横幅 → 三联 → 两两成对）；
5. **过节奏**：套约束（下节），不满足就重抽；
6. **落页**：模板 → 页。

### 底线（随机永不许打破）

- 竖图不进宽槽、超宽图不进会裁切的模板；
- 不足三张不做 T4 / T8，不足两张不做 T6 / T7；
- 跨页类（T1/T2/T3/T4）**不连续出现超过 2 次**（避免"前三页全跨页"）；
- 整本至少出现 3 种模板（图够多时）；
- 同一种模板不连续出现；单页类（T5 / T8 / T9）连续出现是允许的。

---

## 6. 与现有代码的对应

| 位置 | 现在 | 要变成 |
|---|---|---|
| `plan.js · planStudioPages` | 写死的五步规则，种子只用来镜像双图 | 上面第 5 节的"准入 + 分组 + 洗牌 + 节奏" |
| `plan.js · SINGLE_LAYOUTS_BY_ORIENTATION` | Gallery 风格的单页版式池 | 保留，作为 T5/T9 的具体呈现 |
| `plan.js · 上下两张` | 只在 Gallery 里 | 抽出来成为 T6，studio 也能用 |
| `makeStudioSpread` / `triptychBoxes` | 跨页类的盒模型 | 复用；新增 T2 的"内缩"盒模型与 T6/T8 的"一页多张"盒模型 |
| `seed` | 已存在且可复现 | 不变，扩大它的作用范围 |
| 开本 `pageFormat.js` | 用户手选（3:4 / 4:3 / 1:1 / 4:5） | **不动**：不跟着图片比例走，保持默认 + 手选 |

---

## 7. 已定（原「待确认」）

| # | 问题 | 结论 |
|---|---|---|
| ① | T2 的白边 | **四边等宽**（起始 6%，可调） |
| ② | T4 三联出现次数 | **一本一次**（保持现状） |
| ③ | T8 的含义 | **一页之内自上而下三张**（不是跨页缩小版）；参照作品集版面：统一高度、各自保比例、共用中轴、等距，图下留小字位；对页留白 |
| ④ | 节奏参数 | 按第 5 节写的定：跨页类连续上限 2、整本至少 3 种模板、单页类可连续 |
| ⑤ | 开本 | **不跟着图片比例走**。不强制裁切、走自适应；模板只是"根据上传图片自动匹配"的参考。`SLC.md` 原判据不变；**T1 门槛收紧**（只在裁切损失可忽略时用） |
| ⑥ | 模板的适用范围 | **只服务摄影书（studio）**；Gallery / Rhythm / Frame 不动 |
| ⑦ | 随机幅度 | **允许换模块**（同一张图这本做三联、那本做竖图大图） |

---

## 8. 实施顺序

1. **T2 跨页留白 + T6 一页上下两张**（补上缺口，规则简单，立刻能看到效果）；
2. **准入表 + 分组/洗牌/节奏**（把 `planStudioPages` 从"写死顺序"改成"随机生成"）；
3. **T8 一页三张**（本次已定：一页三张，对页留白）；
4. 编辑模式（逐页换模板 → 写进 `overrides`）；
5. 图片说明文字（图下小字）—— 参考图里有，本版不做。
